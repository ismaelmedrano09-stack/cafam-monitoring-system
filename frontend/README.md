# Frontend Cafam Monitoring

Interfaz Vue 3 con TypeScript para el sistema de monitoreo.

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

Verificación:

```bash
npm run typecheck
npm run build
```

Abra `http://localhost:5173` e ingrese con `admin@cafam.test` / `Admin123*`.
